module uim.apps.tests;

@safe:
import uim.apps;

/* bool testController(DController controller) {
  if (!controller) { return false; }

  if (controller.name("Test").name != "test") return false;

  auto app = APPApplication;
  if (controller.app(app).app !is app) return false;

  return true;
} */